defaults:
  - paths: default
  - model: rieformer_base
  - manifold: product_h_s_e
  - flow: joint
  - embedding: qwen3
  - data: wikidata_5m
  - training: pretrain
  - task: kgc_lp
  - ablation: full
  - accelerator: gpu
  - eval: default
  - download: default
  - preprocess: default
  - _self_

seed: 42
project_name: riemannfm
run_name: ${now:%Y%m%d_%H%M%S}
device: ${accelerator.accelerator}

# Loggers: "+" separated names from configs/logger/*.yaml
# Examples: "wandb+csv", "csv", "wandb", "null"
loggers: wandb+csv
