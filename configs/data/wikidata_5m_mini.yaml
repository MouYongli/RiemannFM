slug: wikidata_5m_mini
dataset: wikidata
data_dir: ${paths.data_dir}/wikidata_5m_mini
num_entities: 7833
num_edge_types: 822
max_nodes: 64
max_hops: 2
num_workers: 4
# text_encoder and dim_text_emb are determined at preprocess time.
# The dataset auto-detects the actual dimension from disk.
text_encoder: ${embedding.model_name}
dim_text_emb: 0
val_epoch_size: 500
test_epoch_size: 500
