# @package _global_
defaults:
  - /data: wikidata_5m
  - /training: pretrain
  - /task: kgc_lp
  - _self_

seed: 42
run_name: ablation_efficiency_${now:%Y%m%d_%H%M%S}

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      flow.num_inference_steps: 10,25,50,100,200,500
      training.total_steps: 50000,100000,250000,500000
