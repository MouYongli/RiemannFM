# @package _global_
defaults:
  - override /data: wikidata_5m
  - override /training: pretrain
  - _self_

seed: 42
run_name: ablation_loss_${now:%Y%m%d_%H%M%S}

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      flow: joint,continuous_only,discrete_only
